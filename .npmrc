# Only resolve package versions published at least two weeks ago.
min-release-age=14

# Temporary exception for GHSA-mh99-v99m-4gvg. Remove after 2026-08-13,
# when all pinned patched versions reach the minimum release age.
min-release-age-exclude[]=brace-expansion
