# Only resolve package versions published at least two weeks ago.
min-release-age=14

# Temporary exception for GHSA-mh99-v99m-4gvg. Remove after 2026-08-06,
# when the pinned patched version reaches the minimum release age.
min-release-age-exclude[]=brace-expansion
